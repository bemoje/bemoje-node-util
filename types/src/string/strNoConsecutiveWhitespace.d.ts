/**
 * Removes consecutive whitespace characters in a string and replaces them with a single space.
 * @param string The string to be processed.
 * @example ```ts
 * strNoConsecutiveWhitespace('Hello   World');
 * //=> 'Hello World'
 * ```
 */
export declare function strNoConsecutiveWhitespace(string: string): string;
//# sourceMappingURL=strNoConsecutiveWhitespace.d.ts.map